// script.js
function validatePassword() {
    var inputPassword = document.getElementById("passwordInput").value;
    var correctPassword = "101231"; // 将此处替换为你的密码

    if (inputPassword === correctPassword) {
        alert("I don't want you know sth that you shouldn't find is the reason why this iPad's time-limit password has been set.");;
    }
}
